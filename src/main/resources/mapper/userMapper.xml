<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.angdo.E_Commerce.Dao.UserDao">
    <select id="getUserById" resultType="com.angdo.E_Commerce.Dto.UserDTO" parameterType="com.angdo.E_Commerce.Dto.UserDTO">
        SELECT  user_id
        ,       user_nm
        ,       phone_no
        ,       user_create_dt
        ,       user_email
        ,       user_password
        FROM    commerce_user
        WHERE   user_id  =  #{user_id}
    </select>

    <insert id="insUser" parameterType="com.angdo.E_Commerce.Dto.UserDTO">
        INSERT INTO commerce_user
        (
            user_id
        ,   user_nm
        ,   phone_no
        ,   user_create_dt
        ,   user_email
        ,   user_password
        )
        VALUES
        (
            #{user_id}
        ,   #{user_nm}
        ,   #{phone_no}
        ,   now()
        ,   #{user_email}
        ,   #{user_password}
        )
    </insert>
</mapper>